%a.slot{href: speaker.external_url, target: '_blank'}
  %img.thumbnail{src: speaker.avatar_path}
  .about
    .talk-title
      #{speaker.talk_title}

    %h3
      - speaker.name.split(' ').each do |word|
        %span.name= word
